import 'package:background_custom_painter/widgets/squared_header.dart';
import 'package:flutter/material.dart';

class SquaredHeaderPage extends StatelessWidget {
  const SquaredHeaderPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SquaredHeader(),
    );
  }
}
